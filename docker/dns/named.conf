options {
    directory "/var/cache/bind";
    listen-on { any; };
    allow-query { any; };
    recursion yes;
};

zone "infraboard.local" {
    type master;
    file "/etc/bind/zones/db.infraboard.local";
};
